CREATE TABLE hero (
    id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    health INT NOT NULL,
    attack INT NOT NULL,
    level INT NOT NULL,
    experience INT NOT NULL
);
